# Auria Tweak - build.prop overlay (applied as system props)
# Only set long-lived, safe values; runtime props are in engine.sh.

# Rendering
debug.composition.type=gpu
debug.hwui.renderer=skiavk

# Smoothness
persist.sys.jank_detect_interval=10